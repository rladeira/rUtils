% Generated by roxygen2 (4.1.1.9000): do not edit by hand
% Please edit documentation in R/execute.with.windows.parallel.backend.R
\name{execute.with.windows.cpu.parallel.backend}
\alias{execute.with.windows.cpu.parallel.backend}
\title{CPU parallel processing in Windows OS}
\usage{
execute.with.windows.cpu.parallel.backend(task.to.be.executed, n.cores = NULL,
  ...)
}
\arguments{
\item{task.to.be.executed}{a function representing a task to be executed
with parallel processing.}

\item{n.cores}{the numbers of CPU cores to be used in the parallel
processing. By default, it uses the numbers of cores returned by the
\code{\link[parallel]{detectCores}} function. It must be an integer.}

\item{...}{additional paramaters to be delegated to \code{task.to.be.executed}
function.}
}
\description{
Utility function which executes a task with automatic configured windows parallel
backend. Under the hoods, it uses the \code{\link[doSNOW]{makeCluster}}
function from the \code{doSNOW} package.
}
\details{
This function also saves in the global environment a boolean variable called
\code{windows.parallel.backend} indicating if the parallel backend is
available to use.
}
\examples{
library(foreach)

# a function simulating an useful parallel task
summation <- function(){
   # saves the result in the parent enviroment
   result <<- sum(foreach(i = 1:1000, .combine = "c") \%dopar\% {i*i})
}

execute.with.windows.cpu.parallel.backend(summation)
print(result)
}

